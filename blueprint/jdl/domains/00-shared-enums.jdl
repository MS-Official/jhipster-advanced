enum Status {
  ACTIVE, INACTIVE, PENDING, DELETED
}

enum OtpChannel {
  EMAIL, SMS
}

enum OtpPurpose {
  LOGIN, REGISTER, PASSWORD_RESET, TRANSACTION_APPROVAL
}

enum OtpState {
  CREATED, SENT, VERIFIED, EXPIRED, LOCKED, FAILED
}
